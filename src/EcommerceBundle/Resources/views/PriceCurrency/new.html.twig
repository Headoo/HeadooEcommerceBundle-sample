{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{{ 'hecommerce.pricecurrency.create'|trans({}, 'management') }}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('hecommerce_pricecurrency') }}">
            {{ 'hecommerce.management.backtothelist'|trans({}, 'management') }}
        </a>
    </li>
</ul>
{% endblock %}
