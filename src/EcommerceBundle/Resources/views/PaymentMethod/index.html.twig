{% extends 'HeadooEcommerceBundle::base.html.twig' %}

{% block hecommerce_content %}

    <h1>{{ 'hecommerce.paymentmethod.list'|trans({}, 'management') }}</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>{{ 'hecommerce.management.id'|trans({}, 'management') }}</th>
                <th>{{ 'hecommerce.management.name'|trans({}, 'management') }}</th>
                <th>{{ 'hecommerce.management.actions'|trans({}, 'management') }}</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('hecommerce_paymentmethod_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('hecommerce_paymentmethod_show', { 'id': entity.id }) }}">{{ 'hecommerce.management.show'|trans({}, 'management') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('hecommerce_paymentmethod_edit', { 'id': entity.id }) }}">{{ 'hecommerce.management.edit'|trans({}, 'management') }}</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('hecommerce_paymentmethod_new') }}">
                {{ 'hecommerce.management.createnewentry'|trans({}, 'management') }}
            </a>
        </li>
    </ul>

{% endblock hecommerce_content %}
